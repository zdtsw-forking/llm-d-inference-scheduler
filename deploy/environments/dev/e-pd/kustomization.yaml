# ------------------------------------------------------------------------------
# E/PD (Encode / Prefill-Decode) Development Environment
#
# Deploys encode + combined prefill-decode deployments.
# ------------------------------------------------------------------------------
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../../components/vllm-encode/
- ../../../components/vllm-decode/

patches:
- path: patch-decode.yaml

components:
- ../../../components/overlays/simulator/
