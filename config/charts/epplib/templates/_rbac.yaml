{{- define "inference-extension.rbac" -}}
{{- if .Values.inferenceExtension.monitoring.prometheus.enabled }}
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "llm-d-inference-scheduler.cluster-rbac-name" . }}
  labels:
    {{- include "llm-d-inference-scheduler.labels" . | nindent 4 }}
rules:
- apiGroups:
    - authentication.k8s.io
  resources:
    - tokenreviews
  verbs:
    - create
- apiGroups:
    - authorization.k8s.io
  resources:
    - subjectaccessreviews
  verbs:
    - create
- nonResourceURLs:
    - "/metrics"
  verbs:
    - get
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "llm-d-inference-scheduler.cluster-rbac-name" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "llm-d-inference-scheduler.name" . }}
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "llm-d-inference-scheduler.cluster-rbac-name" . }}
{{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ printf "%s-sa" (include "llm-d-inference-scheduler.name" .) }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llm-d-inference-scheduler.labels" . | nindent 4 }}
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "watch", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ printf "%s-sa" (include "llm-d-inference-scheduler.name" .) }}
  namespace: {{ .Release.Namespace }}
subjects:
- kind: ServiceAccount
  name: {{ include "llm-d-inference-scheduler.name" . }}
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ printf "%s-sa" (include "llm-d-inference-scheduler.name" .) }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "llm-d-inference-scheduler.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llm-d-inference-scheduler.labels" . | nindent 4 }}
---
{{- end }}
