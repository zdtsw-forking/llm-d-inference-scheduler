{{- define "inference-extension.lead-election-rbac" -}}
{{- if gt (.Values.inferenceExtension.replicas | int) 1 }}
---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "llm-d-inference-scheduler.name" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llm-d-inference-scheduler.labels" . | nindent 4 }}
rules:
- apiGroups: [ "coordination.k8s.io" ]
  resources: [ "leases" ]
  verbs: [ "get", "list", "watch", "create", "update", "patch", "delete" ]
- apiGroups: [ "" ]
  resources: [ "events" ]
  verbs: [ "create", "patch" ]
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "llm-d-inference-scheduler.name" . }}-leader-election-binding
  namespace: {{ .Release.Namespace }}
subjects:
- kind: ServiceAccount
  name: {{ include "llm-d-inference-scheduler.name" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "llm-d-inference-scheduler.name" . }}-leader-election
---
{{- end }}
{{- end }}
